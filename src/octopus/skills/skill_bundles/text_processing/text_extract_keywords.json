{
  "name": "text_extract_keywords",
  "version": "1.0.0",
  "category": "text_processing",
  "description": "Extract the most important keywords and key phrases from text. Returns ranked list with relevance scores.",
  "author": "octopus",
  "tags": ["text", "keywords", "extraction", "nlp", "seo"],
  "cost_estimate": 0.0005,
  "success_rate": 0.93,
  "steps": [
    {
      "action": "call_llm",
      "params": {
        "prompt_template": "Extract the top 10 most important keywords and key phrases from the following text. Return as a JSON array of objects with 'keyword' and 'relevance' (0-1) fields:\n\n{input_text}",
        "max_tokens": 300,
        "temperature": 0.2
      },
      "on_error": "retry",
      "description": "Extract keywords with relevance scores"
    }
  ]
}
